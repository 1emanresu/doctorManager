/*判断是否已登录，否则跳转到登录页面   @Author  hongwx*/
var docId = sessionStorage.getItem("doctorId");
var docId = 1;/*此代码只给调试使用，上传服务器需注释*/
if(docId == null || docId == ''){
	window.location.href = 'http://120.76.194.76/DoctorPC/login/login.html';
}
